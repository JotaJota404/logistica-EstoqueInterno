spring:
  application:
    name: estoqueInterno
  datasourcee:
    url: jdbc:h2:mem:estoque
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
